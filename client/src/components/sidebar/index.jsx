// @flow
import React from "react";

import Form from "../form";

import "./styles.scss";

const Sidebar = (props) => {
  return (
    <div className="sidebar">
      <h2 className="">Add a robot</h2>
      <Form className="" handleSubmit={() => {}} name="" catchPhrase="" />
    </div>
  );
};

export default Sidebar;
